import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'dark',
  },
  typography: {
    h1: {
      fontSize: "1rem"
    },
    h2: {
      fontSize: "1rem"
    },
    h3: {
      fontSize: "1.5rem"
    },
    subtitle1: {
      fontSize: "1.5rem"
    },
  }
});